[project]
name = "pubsub-shadow"
version = "0.1.0"
description = "Gossipsub interoperability simulation suite for different libp2p implementations."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.1",
    "networkx>=3.4.2",
    "pydantic>=2.11.4",
    "pyyaml>=6.0.2",
    "tqdm>=4.67",
    "pydot>=4.0.1",
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "ruff>=0.11.9",
]
